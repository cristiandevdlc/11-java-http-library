# Library HTTP Server

Servidor HTTP Java sin framework para consultar y prestar libros.

```powershell
javac LibraryServer.java
java LibraryServer
```

Prueba `http://localhost:8090/books` y `POST /books/{id}/borrow`.
